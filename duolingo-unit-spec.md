# Duolingo 单元讲解生成 Spec v4

## 数据源

`duolingo-zs-en.json`，每条记录：

```json
{ "id": 1, "name": "喜好", "desc": "连词成句", "words": ["Chinese", "English", ...] }
```

### 数据边界

- `name`、`desc` 和 `words` 是生成内容的唯一课程依据；它们能反映单元主题，但不等于完整的课程教案
- 只能据此说明这些词**可以用来练习什么**，不得虚构原课程未提供的题目、对话、教学顺序或官方语法目标
- 当语法重点只能从词表中推测时，使用“本单元可以练习……”“这里值得注意……”等表述，不写“本单元只有一个核心”“本单元正式新增……”之类无法由数据确认的断言
- 若词表不足以支持明确的语法结论，就聚焦场景、词汇关系和交际任务，不强行总结语法

## 输出文件

`duolingo-zs-en-4/{NNN}-{name}.md`

## 文档结构

```markdown
# Unit {NNN}: {name}

> {desc}

## 单词

{逗号分隔的单词列表，原样输出 JSON words 数组}

## 单元讲解

{连贯的单元级叙述，见下方规则}

## Tips

{逐词知识点列表，见下方规则}
```

---

## `## 单词` 规则

- 原样输出 JSON `words` 数组，逗号+空格分隔，一行
- 不加表格、不加音标、不加释义（词典功能由网页端承担）

---

## `## 单元讲解` 规则

### 定位

单元级别的连贯介绍，帮学习者在接触具体单词之前建立整体认知。像老师开课前的 2-3 分钟导入。

### 参考维度（非强制清单，按单元实际内容取舍）

- **场景** — 本单元对应什么真实场景或话题
- **语法/结构重点** — 根据 `desc` 和 `words` 能可靠判断的语言现象（若依据不足则略写或不写）
- **词汇关系** — 这些词之间什么逻辑
- **学完能做什么** — 一句话说明交际任务

### 格式

- 自然段落，中文行文，英文术语/例句内嵌
- 不设字数限制——适合展开就多讲，三言两语能说清就短写
- 不用列表，用连贯散文
- 可以举简短例句辅助说明（用英文，不翻译）
- 例句优先使用当前单元的词；必要时可以使用此前单元出现过的词和最基础的功能词
- 可以使用复数、时态、比较级等合理的词形变化，但不能把尚未讲解的变化说成理所当然
- 不为了造例句引入无关的生词、复杂结构或当前阶段难以理解的表达

### 语气

- 平实、有信息量，像好的教材导读
- 不煽情、不堆砌修辞
- 不逐个罗列单词（那是上一节的事）
- 避免“唯一、必然、完全一致、不需要变化”等过度概括；初学阶段的简化说明也要给未来知识留出余地

---

## `## Tips` 规则

### 定位

零散的、逐词的实用或趣味知识点。和"单元讲解"的宏观视角互补——这里是微观的、跳跃的、一条一个知识点。首先帮助学习者正确理解和使用词语，在可靠的前提下再追求有趣、意外和好记。

### 格式：统一列表

只使用无序列表（`-`），每条 tip 是一个独立知识点。

### 条数

- 不设硬性上下限，条数由内容自然决定
- 原则：为每个**有值得讲解内容**的词写一条 tip；纯功能词（the, is, of, a, to 等）若无独立知识点则跳过
- 一个 15 词单元可能产出 10 条 tip，一个 30 词单元可能产出 20 条——以「有料就写、没料就跳」为唯一标准

### 每条 tip 的结构

```
- **{关键词/短语}** — {讲解}
```

- 加粗部分：本条涉及的单词或短语（英文原形）
- 破折号后：中文讲解，把知识点讲透即止，不设句数限制
- 提供"单词列表里学不到"的额外信息
- 鼓励包含（不限于）：常用搭配、语义边界、易错用法、构词关系、语义演变、可靠的词源或文化背景、中文母语者容易理解的对照、谐音/形近/画面式记忆联想

### 内容优先级

1. 高频用法、常见搭配和语义边界
2. 易混淆辨析与用法陷阱（如 he's vs his、was vs were、中式英语常见错误）
3. 构词关系、词义演变以及能帮助理解一组词的规律
4. 中英对照——跨语言的巧合、翻译中的文化碰撞、中文母语者的独特视角
5. 可靠的词源故事、文化背景和历史画面
6. 谐音、形近、画面感等记忆联想

以上不是排他清单，也不要求每个单元机械覆盖全部类别。任何能让学习者“哦！原来如此”或“哈哈这也行”的知识点都值得写，但正确性和学习价值始终高于戏剧性。

注意：中英对照不必每条都带——当对比确实精彩时自然出现，不要为了“覆盖优先级”而机械地给每条 tip 都加一个中文对比。

### 事实准确性

- 词源、历史、文化、语言传播路径、使用频率和跨语言比较都属于高风险事实；只有在有可靠依据且把握足够高时才写
- 不把“拼写相似、读音相近、故事上说得通”当作同源关系，也不把不同历史词形或不同词根误合并为一个词
- 有争议或尚无定论的内容必须明确写成“可能”“有一种观点认为”“词源尚无定论”，不得把推测写成定论
- 包含具体年代、人物、语言名称、传播路线或“最早、所有、唯一、第几高、从未、必然”等强断言时，必须格外核验；无法确认就删除、弱化或改讲可靠的用法
- 记忆联想可以大胆、有画面感，但必须明确是“可以这样联想”，不能伪装成真实词源、历史或文化事实
- 中文字形、汉字来源和中英巧合也需要遵守同样标准，不使用民间字源替代可靠解释
- 一条 tip 中只要核心故事存疑，就不要靠更多未经确认的细节把故事讲圆；宁可换成可靠的搭配、辨析或用法技巧

### 禁止

- 不写散文段落
- 不在列表末尾加"总结"或"速记"子列表
- 不编造不确定的词源（宁可写用法技巧）
- 不重复"单元讲解"中已说过的语法点（Tips 聚焦单词层面）
- 不制造实际上不存在的歧义、语法规则或跨语言对应关系
- 不用夸张、段子或文化刻板印象代替事实；幽默不能牺牲准确性

---

## 生成注意事项

- Tips 中的英文单词/短语保持原形，不加引号
- 全篇中文行文，英文术语/例句内嵌即可
- 文件编码 UTF-8，末尾换行
- 单元讲解中的例句用英文原句，不加中文翻译（场景已交代清楚）

### 生成后自检

生成完成后逐项检查：

1. `## 单词` 是否与 JSON `words` 数组逐项一致，顺序和大小写均未改变
2. “单元讲解”中的课程结论是否都能由 `desc` 和 `words` 支持，有没有把推测写成官方教学目标
3. 例句是否适合当前阶段，有没有无必要地引入生词、复杂结构或未解释的词形变化
4. 每条 tip 是否提供了真正有用或有趣的增量信息，而不是扩写单词释义
5. 词源、历史、文化、排名和绝对化断言是否可靠；不确定的内容是否已弱化、注明争议或替换
6. 记忆联想是否明确只是联想，没有被包装成真实来源
7. 是否存在为了追求“惊奇感”而牺牲准确性、清晰度或学习价值的句子；若有，重写
